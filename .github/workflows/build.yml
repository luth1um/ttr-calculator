name: Build

on:
  push:
    branches-ignore: ['gh-pages']
  pull_request:
    branches: ['main']

jobs:
  build:
    name: Build 🔧
    runs-on: ubuntu-latest
    steps:
      - uses: DerYeger/yarn-setup-action@master
        with:
          node-version: 24
      - run: yarn build
